# Surface Layer

Read `LAYER_IDENTITY.md` and `SURFACE_COCKPIT_IDENTITY.md` first.

This layer controls the answer surface and allowed internal scopes. It must never become a tunnel router. It works with File Intelligence as the Artifact Cockpit and with Truth to keep responses grounded.
