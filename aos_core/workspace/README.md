# Workspace Layer

Read `LAYER_IDENTITY.md` first.

This layer defines stable workspace gates. The active project slot is not a project. The only current-project payload gate is `workshop/active_project/PROJECT/`. Project names inside that gate are discovered payload, never AOS runtime truth.
