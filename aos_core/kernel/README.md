# Kernel Layer

Read `LAYER_IDENTITY.md` first.

The kernel coordinates identity, intent, surface, file intelligence, truth, policy, planning, enforcement, delivery, and audit. It must not contain project-specific logic.
