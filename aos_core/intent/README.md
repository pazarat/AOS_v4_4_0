# Intent Cognition Runtime

This core layer converts requests into operational cognition. It exists to keep AOS from becoming a maze of routes or a robot that blindly obeys the user's wording.

Use `intent_cognition.py` to build an Intent Cognition Packet. The packet tells the kernel whether truth, artifact cockpit, or goal runtime are needed and why.

The layer must remain project-neutral.
